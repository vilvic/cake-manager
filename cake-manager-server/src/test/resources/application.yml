spring:
  config:
    name: Cake manager server test
  datasource:
    url: jdbc:h2:mem:waracle;MODE=MySQL
    driverClassName: org.h2.Driver
    username: sa
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect